# This file is generated by generate-policy-bot-config.
# Do not edit directly. Run "make .policy.yml" to update.

policy:
  approval:
    - or:
        - and:
            - Workflow .github/workflows/test-a.yml succeeded or skipped
            - Workflow .github/workflows/test-b.yml succeeded or skipped
            - default to approval
approval_rules:
  - name: Workflow .github/workflows/test-a.yml succeeded or skipped
    if:
      file_not_deleted:
        paths:
          - ^\.github\/workflows\/test-a\.yml$
    requires:
      conditions:
        has_workflow_result:
          conclusions:
            - skipped
            - success
          workflows:
            - .github/workflows/test-a.yml
  - name: Workflow .github/workflows/test-b.yml succeeded or skipped
    if:
      file_not_deleted:
        paths:
          - ^\.github\/workflows\/test-b\.yml$
    requires:
      conditions:
        has_workflow_result:
          conclusions:
            - skipped
            - success
          workflows:
            - .github/workflows/test-b.yml
  - name: default to approval
